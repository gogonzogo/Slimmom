// import { node, string } from 'prop-types';

const Section = () => {
    return (
        <Section/>
    )
};

// Container.propTopes = {
//     children: node.isRequired,
//     className: string
// };

export default Section;